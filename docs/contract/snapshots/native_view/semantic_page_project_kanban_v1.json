{
  "semantic_page": {
    "model": "project.project",
    "view_type": "kanban",
    "layout": "auto",
    "kanban_semantics": {
      "title_field": "name",
      "subtitle_field": "manager_id",
      "stage_field": "stage_id",
      "card_fields": ["name", "manager_id", "stage_id", "budget_total"],
      "metric_fields": ["budget_total"]
    },
    "zones": [
      {
        "key": "detail_zone",
        "blocks": [
          {
            "type": "relation_card_block",
            "data": {
              "stages_field": "stage_id",
              "kanban_semantics": {
                "title_field": "name",
                "subtitle_field": "manager_id",
                "stage_field": "stage_id",
                "card_fields": ["name", "manager_id", "stage_id", "budget_total"],
                "metric_fields": ["budget_total"]
              }
            }
          }
        ]
      },
      {"key": "action_zone", "blocks": [{"type": "action_bar_block", "data": {"search": {"group_by": []}}}]}
    ]
  }
}
