# 5.3 用户反馈闭环计划

来源文件：`/home/odoo/workspace/5.3日优化.docx`

本文档沉淀本轮从用户反馈 Word 截图中提炼出的业务问题、已完成改动和后续迭代步骤，后续推进以本文档为基准，避免重复拆解原始附件。

## 处理原则

- 业务事实层只承载模型、视图、权限、菜单，不把流程编排、模拟数据、演示逻辑放回业务事实层。
- 新系统模型要承接历史数据字段，优先用正式模型字段表达历史事实；仅无法归入正式模型的内容保留为历史事实模型。
- 审批不硬阻断业务风险。业务规则不满足时应提示给审批人，由审批人判断是否同意推进；系统只对数据闭环、状态一致性、权限边界做硬约束。
- 所有字段补强都要同时覆盖模型、表单视图、列表视图、访问权限和升级/重建可复现性。
- 对“级联创建、必填但界面不可见、技术默认缺失”导致的保存失败，采用模型层系统默认兜底策略：不取消业务字段语义上的必填，不阻断草稿创建；由系统写入“系统默认（待完善）”值并打标识，真实业务推进前由业务人员补全。

## 系统默认兜底策略

适用范围：

- 级联创建时由父单或来源单应自动带出的字段。
- 技术上 required，但当前表单、弹窗或导入链路不可见/不可编辑的字段。
- 草稿阶段允许先保存，后续业务办理前必须可识别并补全的字段。

当前落地：

- 新增 `sc.system.default.mixin`，提供 `含系统默认值`、`系统默认字段`、`系统默认说明` 三个标识字段。
- 物资链路模型接入系统默认兜底：采购申请、材料进场验收、入库单、出库单、询比价、材料结算及其明细。
- 系统默认值仅用于草稿保存和级联创建不失败，列表/表单会提示该记录含系统默认值。
- 物资链路提交、确认、定价、审批类动作会写入非阻断消息，提示经办人/审批人本单含系统默认兜底值。

当前默认记录语义：

- `系统默认项目（待完善）`
- `系统默认供应商（待完善）`
- `系统默认材料（待完善）`
- `系统默认仓库（待完善）`

后续要求：

- 任何新增正式业务模型，如果存在 required 字段但可能在级联创建、历史重放、快速创建中缺失，必须接入同类策略。
- 不能把系统默认值当成真实业务事实参与最终审批或结算，应在关键业务动作前提示审批人或经办人补全。

## 已闭环

### 费用中心菜单

反馈：费用中心需要包含公司扣款、公司收入、公司支出、备用金、借款单、还款单、费用报销单、项目费用报销单。

状态：已补菜单承载入口，归入财务/费用中心业务分类。

提交：`1fbf2464 Expand feedback-driven business fact coverage`

### 供应商

反馈：

- 供应商菜单需要进入基础设置。
- 列表字段存在英文名，如 `Complete Name`、`Job Position`。
- 需要增加供应商类型。
- 表单字段需要账户信息、账户名称、开户银行、账号、备注、附件、统一社会信用代码、所属地区。
- 历史供应商数量与旧系统有差异，需要后续数据重放核对。

状态：已补供应商账户/信用/地区等事实字段和供应商菜单/视图承载。历史数量差异待模拟生产库重放核对。

本轮继续补齐：

- 供应商列表/表单展示历史供应商编号、历史来源、历史供应商名称、历史删除标识等追溯字段。
- 供应商菜单默认 `active_test=False`，便于对账时同时看到活动与停用供应商，避免旧库数量核对被默认活动过滤误导。
- 搜索视图增加“含历史来源”“历史删除标识”和“按历史来源”筛选/分组。

提交：`1fbf2464 Expand feedback-driven business fact coverage`

### 合同体系边界

反馈：收入合同、支出合同、一般合同、历史合同台账混在一起，字段和菜单定位不清。

状态：

- `construction.contract` 定位为项目收入/支出合同正式模型。
- `sc.general.contract` 定位为公司一般合同。
- `sc.legacy.purchase.contract.fact` 降级为历史事实承载，不再作为新业务审批对象。
- 合同审批范围、动作、序列、菜单和视图已随模型边界调整。

提交：`0a64a090 Normalize contract fact model boundaries`

### 合同字段口径

反馈：

- “合同明细金额”应按旧系统语义改为最终合同价。
- 收入合同台账需展示合同编号、结算金额、开票金额、未开票金额、收款金额、未收款金额、挂靠人、工程地址。
- 支出合同台账需展示结算金额、是否归档、已付款金额、未付款金额、已开票金额、未开票金额。
- 列表字段“合同编号”应表达为“单据编号”。
- 需要录入人、录入数据、经办人等历史承接字段。

状态：正式合同和一般合同已补执行金额、录入/经办、工程地址、归档等字段，并调整核心列表/表单视图。部分字段是否能从旧库全量回填，需在模拟生产库重放后验算。

本轮继续补齐：

- 正式合同列表明确展示 `单据编号` 与旧库 `合同编号`，避免把新系统单据号误认为旧合同号。
- 正式合同表单展示历史单据编号、自编合同编号和历史状态，保留历史合同编号事实的可见性。
- 正式合同列表的结算、开票、未开票、收款、未收款、付款、未付款字段补齐页脚合计，便于台账级核对。

提交：

- `0a64a090 Normalize contract fact model boundaries`
- `1fbf2464 Expand feedback-driven business fact coverage`

### 审批风险语义

反馈：业务规则不满足也应允许审批人判断并同意推进，系统不能因为风险提示阻断业务。

状态：付款申请审批回调已从硬阻断改为风险提示；结算状态、超付、资金基线等风险进入 chatter 提示，审批人仍可通过。保留项目关闭、结算取消等数据闭环硬约束。

提交：`6ae2e44e Align approval gates with advisory review`

### 材料计划

反馈：材料计划列表/表单需增加材料名称、规格型号、单位、计划数量、清单数量、未计划数量、备注、附件。

状态：继续补齐列表承载。表单明细已有材料名称、规格型号、单位、计划数量、清单数量、未计划数量、备注、附件；主列表新增明细汇总字段，避免用户进入详情前无法识别计划内容。

当前进展：

- `project.material.plan` 新增材料名称、规格型号、单位、备注、附件数汇总字段。
- 主列表展示材料名称、规格型号、单位、计划数量、清单数量、未计划数量、备注、附件数。
- 汇总字段来自真实明细行，不在前端造数据；多行时显示前几个不同值并保留“等”提示。

提交：`1fbf2464 Expand feedback-driven business fact coverage`

## 待闭环

### 历史数据承接核对

问题：

- 供应商旧系统截图显示约 5111 条，新系统当前统计不一致。
- 收入/支出合同列表的历史数据存在字段未展示或未回填情况。
- 合同结算、入库单等历史事实是否已完整进入正式模型还需按模拟生产库验证。

当前进展：

- 模拟生产库一键重建已通过，历史连续性探针、业务可用探针、业务 smoke、角色矩阵 smoke、前端 init smoke 全部通过。
- 一键重建流程已在 `history_replay` 后自动执行 `formal_projection_refresh`，避免只重放历史 carrier、不刷新正式投影模型。
- `history.business.usable.probe` 已改为严格门禁：只要存在“有历史源数据但正式投影模型为空”的缺口，探针状态直接失败。
- 当前模拟生产库严格探针通过：`HISTORY_BUSINESS_USABLE_PROBE status=PASS gap_count=0`。
- 正式投影刷新通过：`FORMAL_PROJECTION_REFRESH_PROBE status=PASS gap_count=0`。

已确认投影：

- 材料分类：130624 条历史分类投影到正式 `product.category`。
- 材料明细：2279734 条历史材料明细投影到正式 `sc.material.catalog`。
- 结算调整/扣款：13521 条投影到 `sc.settlement.adjustment`。
- 发票登记、费用报销、收款登记、付款执行、融资贷款、施工日志、待办/审批、资金台账和驾驶舱事实均已纳入正式投影刷新和探针门禁。

不强制投影：

- `sc.material.rfq`、`sc.material.acceptance`、`sc.material.inbound` 这类流程单据，当前没有稳定历史源事实可以还原，不为填表而造业务过程。
- 历史材料事实的正式承载模型是 `sc.material.catalog`；是否批量提升为 `product.template` 属于采购执行主数据策略，不等同于历史事实必须全量投影。

### 收入合同结算

反馈：

- 表单需增加单据日期、发包人、承包人、标题、送审金额、审定金额、审定日期、项目名称、合同名称、合同总额、申请资金金额、工程地址等旧系统字段。
- 列表需增加标题、结算单位、扣款金额、已付款金额、未付款金额、结算说明、附件、录入人。

状态：已补入 `sc.settlement.order` 正式模型，不作为临时历史截图字段处理。

已落地：

- 表单字段：单据日期、发包人、承包人、标题、送审金额、审定金额、审定日期、合同名称、合同总额、申请资金金额、工程地址、录入人、录入数据、结算说明。
- 列表字段：标题、结算单位、扣款金额、已付款金额、未付款金额、结算说明、附件数、录入人。
- 本轮继续补齐列表字段：合同名称、合同总额、送审金额、审定金额、申请资金金额、工程地址，并为金额字段补页脚合计。
- 扣款金额来自已确认结算调整；已付款/未付款继续按付款事实计算。
- 创建和导入时自动同步单据日期/结算日期、审定日期/定案日期，并默认结算单位为往来单位。
- 已增加用户反馈回归测试，覆盖全新库升级、视图加载和关键字段计算。

### 一般合同

反馈：

- 菜单“支出合同/一般合同”需明确为“一般合同（公司）”。
- 列表和表单需增加联系人、联系电话。

状态：模型已具备联系人/联系电话字段，列表/表单已部分展示。菜单命名仍需二次核对是否所有入口都统一为“一般合同（公司）”。

当前进展：

- action、菜单、审批策略名称已使用“一般合同（公司）”。
- 搜索、列表、表单标题已统一为“一般合同（公司）”。
- 列表已加入联系人、联系电话。
- 正式模型新增并展示提交时间；一键重建的通用合同投影写入旧库提交时间，避免该事实只停留在旧承载表。
- 正式模型新增签署状态，并由一键重建从旧库同步写入。
- 一般合同业务标签收敛为审批编号、合同编号、合同签订地点、合同预计签订日期、计划交货或完工日期。
- 表单继续展示签署状态、合同签订地点、合同预计签订日期、计划交货或完工日期、联系人和联系电话等旧系统可投影事实。

### 财务与施工重建列表验收

反馈：

- 一键重建后正式模型已有数据，但列表页无法直接看到关键业务字段，导致用户验收时仍像“数据没有投影完整”。

状态：不新增支撑模型，只把已经进入正式模型、并可由一键重建投影的字段补到列表视图。

已落地：

- 收款收入列表补收款方式、收款账户、票据号、发票引用、已抵发票金额、已抵税额、结算金额。
- 付款执行列表补付款族、付款账户、经办人、发票金额。
- 发票登记列表补单据号、单据日期、合同、结算单、发票代码、税率、开票内容、成本类别、经办人、持票人、核算状态、凭证号。
- 资金对账列表补业务类型、银行账号、账面余额、银行余额。
- 融资借款列表补用途说明、利率/类型、扩展引用、扩展标签。
- 施工日志列表补标题、分类、施工单位、项目经理、天气、现场人数。
- 已增加用户反馈回归测试，防止正式列表视图退回只展示少数字段。
- 资金台账列表/表单补来源类型、旧系统记录ID、旧系统来源，金额列补页脚合计。
- 工作台事项列表补业务编号、业务日期、申请人、经办人、截止日期，便于核对我的待办/我的审批重建结果。
- 驾驶舱业务事实列表补业务编号、申请人、经办人、数量合计、税额合计，便于核对资金/成本驾驶舱投影。
- 材料档案列表补辅助单位、层级、拼音简码、有效状态，便于全量材料明细重建后筛查。
- 费用/保证金列表补收款人、收款账号、开户行、摘要、历史单据号、历史状态，申请金额/批准金额补页脚合计。
- 付款申请明细列表/表单补来源单号、来源类型、来源往来方、来源合同号和历史明细追溯字段，金额列补合计。
- 收款发票台账列表/表单补来源单号、来源表名、金额来源和历史发票追溯字段，发票金额/历史已开票/本次收款补合计。
- 采购申请、材料进场验收、入库单、询比价、材料出库列表补来源业务类型；材料结算列表补来源业务类型并为未税金额、税额、结算金额补合计。
- 考勤记录列表补考勤人数、工时合计和来源业务类型；劳务计划/申请/用工列表补来源业务类型，劳务用工补用工人数合计；劳务结算列表补未税金额、税额、结算金额合计和来源业务类型；劳务价格库列表补来源业务类型。
- 设备计划/申请/使用列表补来源业务类型，设备使用补使用台数合计；设备结算列表补未税金额、税额、结算金额合计和来源业务类型；设备价格库列表补来源业务类型。
- 分包计划/申请/登记列表补金额合计和来源业务类型；分包结算列表补未税金额、税额、结算金额合计和来源业务类型；分包价格库列表补来源业务类型。
- 计划/计划汇报列表补来源业务类型，计划节点和计划版本内嵌明细补来源业务类型，便于重建后核对旧系统计划事实来源。
- 合同履约事件列表补来源业务类型、不含税金额、税额、变更控制上限和对应合计。
- 质量标准、质量问题、质量整改、质量复验补来源业务类型可见性，质量标准和质量复验表单补来源追溯页。
- 安全方案、安全交底、危险源、安全问题、安全整改、安全复验补来源业务类型可见性，安全方案、交底、问题、复验表单补来源追溯页。

### 询比价

反馈：

- 表单需增加联系人、联系电话、备注。
- 同一询价需要支持多家供应商同时展现。

状态：已补到正式材料询比价模型，并补齐从物资计划生成询比价的正式入口。

已落地：

- 主表保留本次询价经办联系人、联系电话、备注。
- 报价明细按“供应商 + 材料 + 数量 + 单价”承载多家供应商同时报价。
- 报价明细新增供应商联系人、供应商联系电话、报价状态，默认从供应商档案带出。
- 表单报价明细直接展示多供应商、联系方式和报价状态。
- 物资计划生成询比价向导新增“其他参与供应商”，生成时按供应商展开报价明细，主供应商自动纳入参与范围。
- 用户反馈测试覆盖同一询价单两家供应商同时报价。

### 入库单

反馈：

- 明细需增加单价、金额、合计、备注。
- 表单需增加备注。
- 填单后保存失败，且返回后已填数据丢失。

状态：已完成当前反馈范围内的保存兜底和金额字段承接。

当前进展：

- 入库单已接入模型层默认兜底：缺项目、仓库、库位时自动填入系统默认并打标识。
- 入库明细缺材料、数量时自动填入系统默认材料和数量 1，并打标识。
- 明细已具备单价、金额、备注，表单列表已展示系统默认标识。
- 主列表新增材料名称、规格型号、单位、入库数量合计、单价、备注等明细汇总字段；列表金额列保留金额合计。
- 入库单从验收单带入时，承接验收明细、材料、规格、单位、合格数量、申请预计单价和备注。
- 用户反馈测试覆盖“缺仓库/库位/明细材料也可保存草稿”和“金额合计正确计算”。

剩余核对：

1. 用真实用户账号在前端创建一张入库草稿，确认页面交互不再出现保存失败和数据丢失。
2. 当前没有稳定历史入库单源事实投影到 `sc.material.inbound`，不硬造入库单；如果后续确认旧库入库事实源表，再补正式投影脚本。

### 材料进场验收/采购链路

反馈：材料计划、采购申请、材料进场验收、采购订单、询比价、入库单之间需要更完整的字段承接。

状态：已完成当前反馈范围内的正式链路承接。

当前进展：

- 采购申请明细可带入材料进场验收，承接材料、规格、单位、申请数量、备注。
- 验收明细新增来源申请明细锚点，后续可追溯申请单事实。
- 入库单从验收单带入时，会继续承接来源验收明细、材料、规格、单位、合格数量、申请预计单价和备注。
- 材料计划“生成询价单”向导已改为生成内部正式 `sc.material.rfq`，不再误生成 Odoo 采购订单。
- 询比价主表新增来源材料计划，报价明细新增来源计划明细，承接材料、规格、单位、计划数量、建议供应商和备注。
- 询比价可按选中报价、选定供应商或有效报价生成 Odoo 采购订单。
- 采购订单新增来源询比价，采购订单明细新增来源询价明细，继续承接来源材料计划和计划明细。
- 材料进场验收可从采购订单带入供应商、项目和采购明细，验收明细新增来源采购明细锚点。
- 用户反馈测试覆盖“采购申请 -> 材料进场验收 -> 入库单”和“材料计划 -> 询比价 -> 采购订单 -> 材料进场验收”字段传递链路。

剩余核对：

1. 模拟生产库一键重建后，对材料计划、询比价、采购订单、验收单、入库单做历史样本抽查。
2. 如旧库存在材料计划直接生成采购申请的历史动作，再补对应重放映射；当前正式交互链路已先覆盖材料计划生成询比价。

## 推荐执行顺序

1. 先修复入库单保存失败。该问题直接影响用户填单，是当前最高优先级。
2. 补入库单金额字段和备注字段。
3. 补合同结算字段和视图。
4. 补询比价多供应商能力。
5. 统一一般合同菜单和联系人/电话列表列。
6. 在模拟生产库做一键重建，核对历史数据数量和字段映射。
7. 补测试并分类提交。

## 验收清单

- 供应商、合同、结算、材料计划、询比价、入库单页面列表字段均为中文业务字段。
- 历史数据重放后关键模型数量与旧系统口径可解释一致。
- 表单创建失败时有明确错误，且不丢失用户已填内容。
- 审批业务风险只提示不阻断，审批人可以继续推进。
- 数据闭环硬约束仍生效，例如项目关闭前未完成结算/付款不可直接关闭。
- 全新重建和模块升级后字段、视图、权限、菜单均自动生效。
