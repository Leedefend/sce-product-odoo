
# 📘 Smart Construction Git 分支命名风格指南（中文版完整版）v1.1

**适用范围：**
Smart Construction（智能工程项目管理系统）后端 Odoo 核心、前端 Vue3、未来多模块仓库。

**目标：**
在保证 Git 工作流规范化的同时，使分支命名与业务语义保持高度一致，提高可读性、可维护性与跨阶段协作效率。

---

# 1. 分支命名的基本原则

Smart Construction 项目允许 **中英文混合命名分支**，但必须满足：

1. **结构统一**（固定格式 `type/identifier-description`）
2. **英文在前 → 工具兼容性最佳**
3. **中文在后 → 业务语义最直接**
4. **避免纯中文或纯拼音**（国际化工具兼容性弱）

推荐格式：

```
<type>/<identifier>-<中文业务描述>
```

示例：

```
feat/boq-import-phase2-清单导入增强
feat/settlement-center-结算中心二期
fix/boq-total-总价措施合计修复
refactor/core-核心架构重构
docs/wbs-design-工程结构设计说明补充
```

---

# 2. 分支类型（type）定义

| 类型       | 说明             | 示例                    |
| -------- | -------------- | --------------------- |
| feat     | 新功能开发          | feat/boq-import-清单导入  |
| fix      | Bug 修复         | fix/boq-total-合计金额修复  |
| refactor | 重构（不改变外部逻辑）    | refactor/core-结构优化    |
| perf     | 性能优化           | perf/treeview-性能提升    |
| docs     | 文档相关           | docs/readme-更新总览文档    |
| chore    | 其他任务（构建、脚本、CI） | chore/makefile-优化构建脚本 |

---

# 3. 分支命名结构说明

完整结构：

```
type / identifier - 中文业务描述
```

字段解释：

### 3.1 type（英文）

表示分支目的，如 feat / fix / refactor。

### 3.2 identifier（英文）

通常概括模块或功能缩写，如：

* `boq` → 工程量清单
* `settlement` → 结算
* `contract` → 合同
* `core` → 核心架构
* `import` → 导入能力
* `wbs` → 工程结构（Work Breakdown Structure）

### 3.3 description（中文）

简短清晰描述业务目标，如：

* 清单导入增强
* 总价措施修复
* 工程结构生成优化
* 结算中心第二阶段

---

# 4. 分支命名示例库（可直接使用）

以下是 Smart Construction 项目常见场景的标准命名模板。

---

## 4.1 工程量清单（BOQ）方向

```
feat/boq-import-phase2-清单导入增强
feat/boq-summary-工程量汇总提升
fix/boq-total-总价措施合计修复
refactor/boq-model-清单模型结构优化
```

---

## 4.2 工程结构（WBS）方向

```
feat/wbs-generate-工程结构生成逻辑升级
feat/wbs-view-树状结构展示优化
refactor/wbs-service-结构服务重构
```

---

## 4.3 合同管理（Contract）

```
feat/contract-payment-合同付款节点
fix/contract-calc-合同金额计算修复
refactor/contract-model-合同模型优化
```

---

## 4.4 结算中心（Settlement）

```
feat/settlement-center-结算中心二期
feat/settlement-order-结算单生成
fix/settlement-amount-结算金额错误修复
```

---

## 4.5 物资管理与计划

```
feat/material-plan-物资计划生成
feat/material-request-物资需求智能化
fix/material-uom-单位转换错误修复
```

---

## 4.6 核心架构（Core Refactor）

```
refactor/core-engine-结构驱动引擎重构
refactor/core-intent-意图解析机制升级
feat/core-service-新增后端服务层
```

---

## 4.7 文档、构建、CI

```
docs/boq-design-清单导入设计说明
docs/core-blueprint-核心架构蓝图
chore/makefile-新增升级指令
chore/ci-引入lint检查
```

---

# 5. 推荐实践：分支描述尽量精准

不建议：

```
feat/new-feature
fix/bug
feat/test
```

建议：

```
feat/boq-import-phase2-支持合并单元格解析
fix/boq-total-修复小计行重复计入问题
feat/settlement-center-新增结算科目体系
```

---

# 6. 分支长度与可读性

建议控制分支名长度在 **40–80 字符之间**，保证：

* Git 输出不会过长
* 业务语义足够表达
* PR 列表清晰可查

示例完美长度：

```
feat/boq-import-phase2-清单导入增强
```

---

# 7. 禁止事项（重要）

为保持仓库一致性：

### ❌ 禁止纯中文分支名

如：

```
feat/清单导入增强
```

问题：

* URL 中被编码，不易阅读
* 工具链兼容性差

### ❌ 禁止拼音分支名（表达含糊）

```
feat/qingdan-daoru
```

### ❌ 禁止无意义的分支名

```
feat/temp
fix/aaa
update/123
```

---

# 8. 最佳实践总结（TL;DR）

**最推荐格式：**

```
feat/<module>-<submodule>-<中文描述>
```

示例：

```
feat/boq-import-phase3-支持其他项目清单解析
fix/boq-total-修复合计行跳过逻辑
refactor/core-优化WBS构建算法
```

---

# 9. 结合 PR 流程的工作方式

### 1) 从 main 开新分支：

```bash
git checkout main
git pull
git checkout -b feat/boq-import-phase2-清单导入增强
```

### 2) 开发并推送：

```bash
git push -u origin feat/boq-import-phase2-清单导入增强
```

### 3) 创建 PR（自动生成标题）：

```
feat(boq): 清单导入增强（phase 2）
```

### 4) review → merge → 删除分支。

---

# 📌 附录：常用模块缩写（identifier 建议）

| 模块    | 缩写         | 示例                     |
| ----- | ---------- | ---------------------- |
| 工程量清单 | boq        | feat/boq-import-phase2 |
| 工程结构  | wbs        | feat/wbs-generate      |
| 结算中心  | settlement | feat/settlement-center |
| 合同    | contract   | feat/contract-payment  |
| 物资计划  | material   | feat/material-plan     |
| 成本中心  | cost       | feat/cost-ledger       |
| 核心架构  | core       | refactor/core-engine   |

---

# 📘 Smart Construction Git 分支命名风格指南 v1.1 到此结束


