# TP-01｜项目中心分区映射表（A1 → 页面现实）

本表用于把当前项目中心页面中的页签/区块，映射到 A1 定义的四大分区。
仅做“归类 + 是否对 PM 展示 + 表达密度 + 备注”，不涉及权限实现细节。

## 页签 / 区块映射

| 页签 / 区块 | 所属分区 | 是否对 PM 展示 | 表达密度 | 备注 |
| --- | --- | --- | --- | --- |
| 项目概览（sc_overview） | 概览区 | 是 | 低～中 | 包含项目基本信息与关键指标摘要 |
| 项目概览卡片（pay_* 指标卡） | 概览区 | 是 | 低 | 只读汇总，避免深度明细 |
| 施工信息（sc_construction） | 概览区 | 是 | 低～中 | 项目基础信息与责任人 |
| 投标管理（sc_tender） | 执行区 | 条件性展示 | 低 | PM 只读摘要（状态/中标/金额），不进明细 |
| WBS结构 | 执行区 | 条件性展示 | 低 | PM 只读汇总（节点数/完整性），不展开树 |
| 工程量清单（boq_tab） | 执行区 | 条件性展示 | 低 | PM 只读状态/总额/版本，不进明细 |
| 工程结构 | 执行区 | 否 | 中 | 仅成控/工程线角色 |
| 合同 | 管理支撑区 | 是 | 低 | PM 只读汇总，不含配置动作 |
| 成本与进度（成本/进度指标组） | 管理支撑区 | 是 | 低 | 只读指标，避免成控明细 |
| 驾驶舱 | 管理支撑区 | 是 | 低～中 | 高阶摘要区；PM 可见（只读） |
| 工程资料 | 协作与系统区 | 是 | 低 | 只读资料状态 |
| 协作 / 系统 | 协作与系统区 | 是 | 低 | 系统字段区，保持在末尾 |

## 按钮 / 快捷入口映射（项目表单按钮区）

| 入口 | 所属分区 | 是否对 PM 展示 | 表达密度 | 备注 |
| --- | --- | --- | --- | --- |
| 工程结构 | 执行区 | 条件性展示 | 低 | PM 只读视图或汇总入口 |
| 工程量清单 | 执行区 | 条件性展示 | 低 | PM 只读状态/汇总入口 |
| 预算/成本 | 管理支撑区 | 是 | 低 | PM 只读汇总入口 |
| 合同 | 管理支撑区 | 是 | 低 | 只读汇总入口 |
| 物资/采购 | 管理支撑区 | 是 | 低 | 只读汇总入口 |
| 结算/财务 | 管理支撑区 | 是 | 低 | 只读汇总入口 |

## 冻结/调整建议（仅记录，不执行）

- 执行区内“工程量清单/工程结构/WBS”对 PM 默认不展示，避免触发跨域读取。
- 管理支撑区的合同/成本/财务仅保留只读摘要与趋势，杜绝配置入口。
- 协作与系统区始终置于末尾，不参与主流程导航。
