# FE Compat Bridge Retirement Screen EL

```json
{
  "next_candidate_family": "compat_bridge_live_ingress_evidence",
  "family_scope": [
    "docs/audit/fe_compat_bridge_retirement_scan_ek_v1.md",
    "docs/ops/iterations/delivery_context_switch_log_v1.md"
  ],
  "reason": "The fresh inventory shows residual bridge registration and compat recognition still exist, but the strongest blocker against retirement is not structural complexity alone. The dominant fact is that earlier bounded live verification already proved a real legacy compat-record URL could still enter the running frontend. That means the next legal batch is not immediate retirement implementation and not even a pure structural readiness screen; it is a bounded evidence line that decides whether legacy saved-link or live ingress obligations still remain. Until that evidence is refreshed or explicitly retired, compat bridge removal would still be assumption-driven."
}
```
