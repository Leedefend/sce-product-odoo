# BE Generic Action Route Supply Screen EZ

```json
{
  "next_candidate_family": "menu_target_interpreter_scene_known_action_targets_first",
  "reason": "After enterprise bootstrap closure, the strongest remaining generic `/a/...` publication seam is still `addons/smart_core/delivery/menu_target_interpreter_service.py`. The smallest next closure is not all action targets at once; it is only the action-target branches where existing scene maps already derive a stable `scene_key` and route. Those branches can be republished through formal `entry_target` scene semantics first, while unresolved action targets remain compatibility-shaped until a later batch proves stronger scene supply.",
  "decision": {
    "next_slice": "smart_core/delivery/menu_target_interpreter_service.py scene-known action targets first",
    "full_generic_action_removal_now": false
  }
}
```
