import {
  createRecord,
  listRecords,
  readRecord,
  writeRecord,
} from '../../api/data';

export const createContractFormRecord = createRecord;
export const listContractFormRecords = listRecords;
export const readContractFormRecord = readRecord;
export const writeContractFormRecord = writeRecord;
