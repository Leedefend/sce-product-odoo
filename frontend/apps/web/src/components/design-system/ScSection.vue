<template><section class="sc-design-section" :aria-labelledby="title ? titleId : undefined" :aria-label="label"><header v-if="title || $slots.actions"><h2 v-if="title" :id="titleId">{{ title }}</h2><slot name="actions" /></header><p v-if="description" class="sc-muted">{{ description }}</p><slot /></section></template>
<script setup lang="ts">import { useId } from 'vue'; defineProps<{ title?: string; description?: string; label?: string }>(); const titleId = `sc-section-${useId()}`;</script>
<style scoped>.sc-design-section{display:grid;gap:var(--sc-product-space-2)}.sc-design-section>header{display:flex;align-items:center;justify-content:space-between;gap:var(--sc-product-space-2)}.sc-design-section h2,.sc-design-section p{margin:0}.sc-design-section h2{font-size:var(--sc-product-text-section)}</style>
