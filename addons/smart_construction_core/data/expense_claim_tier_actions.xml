<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="0">

        <record id="server_action_expense_claim_on_approved" model="ir.actions.server">
            <field name="name">费用/保证金审批通过回调</field>
            <field name="model_id" ref="model_sc_expense_claim"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
target_records = records or record
if not target_records and env.context.get("active_model") == "sc.expense.claim" and env.context.get("active_id"):
    target_records = env["sc.expense.claim"].browse(env.context["active_id"])
if target_records:
    target_records.action_on_tier_approved()
]]></field>
        </record>

        <record id="server_action_expense_claim_on_rejected" model="ir.actions.server">
            <field name="name">费用/保证金审批驳回回调</field>
            <field name="model_id" ref="model_sc_expense_claim"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
target_records = records or record
if not target_records and env.context.get("active_model") == "sc.expense.claim" and env.context.get("active_id"):
    target_records = env["sc.expense.claim"].browse(env.context["active_id"])
if target_records:
    target_records.action_on_tier_rejected()
]]></field>
        </record>

    </data>
</odoo>
