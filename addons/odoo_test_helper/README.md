# odoo_test_helper stub

Minimal helper placed on the addons path so imports like `import odoo_test_helper` succeed in tests without coupling business modules to test-only code. There is intentionally no Odoo manifest; this package only needs to be importable by Python for test runners.
